# Agent instructions

- Prefer the defaults provided by the imported Paper.css theme for typography, colors,
  spacing, controls, and document layout.
- Avoid adding custom styling unless it is extremely necessary for the game’s layout
  or interaction. When custom CSS is necessary, keep it scoped to the game component
  and do not override Paper.css globally.
